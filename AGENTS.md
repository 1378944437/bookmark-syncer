# marksync 工作流程规范

本文件是本项目的代理工作规范，改编自通用《工作流程规范》。架构详情以 `REFACTORING.md` 为权威正文，本文件只引用其操作性红线。

## 1. 项目速览

- 隐私优先的 WebDAV 书签同步浏览器扩展（Chrome / Firefox / Edge），支持增量同步与本地快照备份。
- pnpm monorepo（`packageManager: pnpm@10.26.2`）：
  - `packages/app`（`@marksync/app`）：全部核心逻辑，DDD 分层（`infrastructure` / `core` / `application` / `components` / `background`），测试在 `tests/`，目录镜像 `src/` 分层。
  - `apps/chrome-extension`、`apps/firefox-extension`：两个壳工程，仅入口与 manifest。
- 技术栈：TypeScript（strict）+ React 19 + Vite 5 + Vitest 4 + Tailwind 3。
- 当前目录存在 `.git` 仓库（2026-09-14 核对）；默认不初始化、提交、推送或发布。
- 没有任何 lint / 格式化工具链；静态质量门为 tsc strict，另有 Vitest 与构建检查。

## 2. 基本原则

- 目标决定范围，风险决定流程，证据决定完成。
- 最小必要变更，保护已有成果，不混入无关整改。
- 减少重复检索、无效重试与冗余记录，不省略必要验证。
- 区分事实、假设、方案和历史记录；不确定的信息明确标注。

## 3. 任务分级（本项目示例）

开始前明确目标、对象、范围、执行方式、验收标准和授权边界。执行方式分为只读分析、拟定方案和实施；讨论方案不自动授权实施。按影响范围、可逆性、外部副作用及验证不确定性中的最高风险分级：

| 等级 | 本项目典型场景 | 最小流程 |
| --- | --- | --- |
| 轻量 | README / 注释 / i18n 文案微调等单点可逆小修 | 确认 → 执行与核对 → 交付 |
| 标准 | `packages/app/src` 业务逻辑、UI 组件、测试、manifest 版本号 | 明确验收 → 简短计划 → 实施 → 验证 → 交接 |
| 高风险 | `core/sync` 合并与冲突解决、`core/backup` 快照恢复、`infrastructure` 加密与 webdav-client 契约、`.github/workflows` 发布链路、`package` / `sign` 打包脚本 | 标准流程，加方案确认、影响分析、切换顺序及恢复或损失控制安排 |

文件数量、任务名称和预计工时不直接决定等级。范围或风险变化时重新评估。

## 4. 修改前检查

- 只读取必要上下文；恢复任务时重新核对现实状态。
- 确认目标目录、已有修改及未跟踪内容，明确保护方式。
- 版本号单一来源：根 `package.json` 的 `version`，被两个 `manifest.config.ts` 直接引用；改版本只改根。
- 架构权威正文是 `REFACTORING.md`；其与代码现状冲突时以代码为准，并在交接中说明偏差。
- 确认维护源、生成产物（`dist/`）、共享消费者和允许的验证路径。
- 高风险操作具备明确的停止条件、恢复依据或已接受的损失控制方案。
- 仅对影响目标、范围、授权或验收的阻塞性歧义请求确认。

## 5. 执行与授权

- 在已授权范围内自主完成常规实现决策，不重复请求已有授权。
- 提交、推送、历史改写、资源删除和部署等操作须满足当次明确授权及平台审批要求；**本项目默认不 git init / commit / push**，除非明确授权。
- 方案、模板、历史记录不构成额外操作授权。
- 同一文件发生并行修改、可能覆盖成果或关键依赖不清时，暂停相关写入；独立且安全的部分继续。
- 不擅自重置、暂存、切换分支或整库格式化来清理工作环境；`dist/` 等构建产物不手工修改。
- 外部写入结果未知时，先核对实际状态，避免重复执行；不得换工具绕过权限拒绝。

## 6. 验证与证据

按验收目标、受影响行为和项目要求选择检查，不默认全量回归，也不只检查修改行。可用命令：

| 检查 | 命令 | 能证明的范围 |
| --- | --- | --- |
| 单元测试 | `pnpm test`（= `pnpm --filter @marksync/app test`，Vitest） | 已执行用例覆盖的行为 |
| 构建 + 类型检查 | `pnpm build`（全量）；`pnpm build:chrome` / `pnpm build:firefox`（内含 tsc strict） | 指定环境能生成产物、类型一致 |
| 开发运行 | `pnpm dev:chrome` / `pnpm dev:firefox` | 扩展在浏览器中的实际表现（人工检查） |
| 打包 | `pnpm package`（Firefox 签名需 `.env` 凭证） | 发布产物可用 |

- 没有独立 lint 步骤；不要假设 eslint / prettier 存在。
- 结果分别标记为通过、失败、未执行、无法执行、不适用；缺失证据不能算通过。
- 有效证据可以复用，但须说明来源与适用范围，不写成此次重新执行。
- 内容、依赖或环境变化导致证据失效时，仅补验受影响部分。
- 重试须有修正、新证据或明确的暂时性故障依据；无可检验的新尝试时报告阻塞。

## 7. 架构约束（权威正文：REFACTORING.md）

- DDD 分层依赖方向：`components` / `background` / `application` → `core` → `infrastructure`；core 不依赖 infrastructure 具体实现，面向 `IStorageProvider` 等接口。
- 每个文件不超过 300 行，保持单一职责。
- 新增测试放在 `packages/app/tests/`，目录镜像 `src/` 分层。
- 浏览器 API 一律经 `webextension-polyfill` + `infrastructure` 封装访问；测试运行在 node 环境，依赖 `src/__mocks__/`。
- 已知技术债：sync strategies 之间存在 Rollup 循环依赖警告，改动时不得使其扩散。

## 8. 文档与脱敏

- 每类规则或长期结论只维护一个权威正文（架构 → `REFACTORING.md`，流程 → 本文件），其他入口引用它。
- 当前规则、待实施方案和历史证据分别管理；文档状态不代替任务验收。
- 维护源与生成产物保持对应；必要时保留输入、生成方式、依赖和验证信息。
- WebDAV 服务器地址、账号、令牌、Firefox 签名 API key / secret 及完整敏感日志不进入文档、模板或交接材料；示例使用通用名称（如 `https://dav.example.com`）。
- README 有中英两份（`README.md` / `README_en.md`），面向用户的改动需同步两份。
- 脱敏时保留影响结论的条件、限制和证据性质，不把未验证内容改写为确定事实。

## 9. 完成与交接

完成需同时满足：目标达到、必要验收通过、范围可解释、已有成果保留、证据覆盖最终内容、必要说明同步、约定人工验收完成。

缺少必要证据时交付候选结果，并明确「待验证」。暂停或恢复任务时保留进度、阻塞原因、解除条件与恢复依据。

统一交接格式：

- **结果**：完成了什么，对应哪些验收目标。
- **验证**：实际检查、复用证据及其结果。
- **限制**：未验证内容、失败项和不确定性。
- **交付状态**：保存、提交、推送、发布分别说明；本项目无 .git 时写「已保存（未纳入版本控制）」。
- **下一步**：剩余工作、解除阻塞条件及所需授权。

成本优化用于选择执行策略，优先减少重复和无交付价值的工作；不得降低验收标准、扩大预算或以预估代替实测收益。
